{
  "name": "k-skill-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Прокси на Fastify для бесплатных и публичных API, используемых в ru-skill",
  "license": "MIT",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "lint": "node --check src/airkorea.js && node --check src/server.js && node --check test/airkorea.test.js && node --check test/server.test.js",
    "test": "node --test"
  },
  "dependencies": {
    "fastify": "^5.3.3"
  }
}
