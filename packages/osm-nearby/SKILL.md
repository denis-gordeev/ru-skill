---
name: osm-nearby
description: Поиск ближайших заведений (рестораны, кафе, бары) через публичный программный интерфейс Overpass OpenStreetMap без ключа программного интерфейса и проверки подлинности.
license: MIT
metadata:
  category: еда
  locale: ru-RU
  phase: v1
---

# Поиск поблизости OSM

Поиск ближайших заведений (рестораны, кафе, бары) через публичный программный интерфейс Overpass OpenStreetMap.

## Что делает навык

Пакет `osm-nearby` использует публичный программный интерфейс Overpass OpenStreetMap для поиска ближайших только для чтения:

- Искать ближайшие заведения по координатам в радиусе до 40 км
- Фильтровать по категориям: рестораны, кафе, бары, фастфуд и другие типы заведений
- Возвращать детали: название, адрес, телефон, сайт, часы работы, тип кухни
- Работать без ключа программного интерфейса и без проверки подлинности

## Когда использовать

- «Найди рестораны рядом со мной»
- «Покажи кафе в радиусе 1 км от центра Москвы»
- «Какие бары есть поблизости от 55.75, 37.61»
- «Нужно бесплатное решение для поиска ближайших без ключа программного интерфейса»

## Предварительные условия

- Node.js 18+
- После публикации: `npm install -g osm-nearby`
- Перед запуском: `export NODE_PATH="$(npm root -g)"`
- При разработке в этом репозитории: `npm install` в корне

## Входные данные

- `lat` (number): Широта
- `lon` (number): Долгота
- `options` (object, необязательно):
  - `radius` (number): Радиус поиска в метрах (по умолчанию: 1000)
  - `categories` (string[] | 'restaurant' | 'cafe' | 'bar'): Типы заведений (по умолчанию: ['restaurant', 'cafe', 'bar'])
  - `limit` (number): Максимум результатов (по умолчанию: 20)

## Рабочий процесс

### 0. Установить пакет глобально, если отсутствует

Если `node -e 'require("osm-nearby")'` не проходит, сначала ставится пакет.

```bash
npm install -g osm-nearby
export NODE_PATH="$(npm root -g)"
```

### 1. Поиск ближайших заведений

```javascript
const { searchNearby, searchRestaurants, searchCafes, searchBars } = require("osm-nearby");

const restaurants = await searchRestaurants(55.7558, 37.6173, {
  radius: 1000,
  limit: 15
});
```

### 2. Детали конкретного заведения

```javascript
const { getPlaceDetails } = require("osm-nearby");

const place = await getPlaceDetails("node/123456789");
```

## Критерии завершения

- Найдены ближайшие заведения по координатам с указанием названия, адреса и типа
- Результаты отсортированы по расстоянию и содержат доступные метаданные
- Источник явно назван программным интерфейсом Overpass OpenStreetMap

## Возможные ошибки

- Нет рейтингов и отзывов (в отличие от курируемых сервисов)
- Качество данных варьируется по регионам (лучше в крупных городах)
- Не все заведения имеют полные метаданные (телефон, сайт и т.д.)

## Примечания

- Используется [Overpass API](https://overpass-api.de/) для запросов к данным OpenStreetMap. Ключ программного интерфейса не требуется.
- Основная конечная точка: `https://overpass-api.de/api/interpreter`
- Лимиты: ~10,000 запросов/день
- Лицензия данных: [ODbL](https://www.openstreetmap.org/copyright)
